# 미적분학이 직관을 말아먹은 학문이 아니고서야, 적분이 하나임.

D = d/dx라 치자,
Fact) x ∈ dom D⁻¹임은 기본적으로, x의 원시함수가 미분 가능하다는거임.

i) D⁻¹가 적분이라면
적분 가능성 == 원시함수의 미분 가능성

직관적인 시나리오지.

ii) D⁻¹는 적분에서 정의역을 제한한거지, 적분이 아닌거라면
적분은, 미분의 역과정을 포괄하는 더 상위의 연산일것임.

비직관적인 시나리오지.

다시 돌아와서,

th. 두 시나리오에 대해, 다음 결론이 나온다.
1. 문장 "적분 가능성 == 원시함수의 미분 가능성"과 동치인 모델에선, D⁻¹가 적분임.
2. 문장 "적분 가능성 ⊇ 원시함수의 미분 가능성"과 동치인 모델에선, D⁻¹이 적분이 아니고, 적분에서 정의역을 원시함수의 미분 가능성이 참인 함수들의 집합으로 줄여야 함.

결론 : 미적분학이, 직관을 말아먹은 학문이 아니고서야, f ∈ {df/dx | D¹ f}가 적분 가능한 f를 이르는 말이여야 함. 안그러면, "적분 가능성 ⊇ 원시함수의 미분 가능성"과 동치인 모델은 한두개가 아님. ㅈㄴ 여러개일거임

## 그러나 인간은 적분으로 크기를 재고싶은 욕망을 이기지 못했다.

이런 비근본주의자 자식들... 엄밀성이 있으면 지랄을 하든 정상화를 하든 좋지만, 왜 하필 꼭 지랄을 할까.

Fact) 적분 가능성 D⁻ⁿ는 다음 문장을 만족시켜야 한다. "f ∈ {dⁿ f/d xⁿ | Dⁿ f} → D⁻ⁿ f"

그런데 그 역이 안되면 어쩌라고 미친놈들이 적분의 정의를 어떤식으로 만든건데?

(검색중) 흠... 진짜 저 문장만 만족시키면 다 적분이라고?

X ≤ₐ y : (a → (x ≤ y)) ∧ ((¬a) → (x < y))
x = D⁻¹
t = {(f, x, y) | D⁻¹ f ∧ (x, y) ∈ (dom f)²}
integral|ₓ : x ↦ ran integral
integral|ₜ : t ↦ ran integral
IntegralAll : x ∩ {f | inf dom f ≠ -∞, sup dom f ≠ ∞} ↦ran integral
IntegralAll(f) = integral|ₜ(f, inf dom f, sup dom f)
에서,

1. integral|ₓ는 선형
2. "∀x ∈ s, f(x) > g(x)" → integral|ₓ(f|ₛ) > integral|ₓ(g|ₛ)
3. (∀i, j, k, l ∈ 𝔹, p = {x | a ≤ₗ x ≤ⱼ c} ∧ s = {x | c ≤ₖ x ≤ₗ b} ∧ r = p ∪ s = {x | a ≤ᵢ x ≤ₗ b}) → IntegralAll(f|ᵣ) = IntegralAll(f|ₚ) + IntegralAll(f|ₛ)

이딴것만 만족해도 적분?

아....

미친놈들;;